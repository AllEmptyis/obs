- [x] 전북도청 펌웨어 안내
- [x] 전주대학교 일감 업데이트
- [x] 전주대학교-추가문의사항 전달
	- 힙 메모리 늘리는 방법
```
Enter the number of your choice: 3
[root] Input : 3
[root] LOCAL CMD || [ -d /cproject ] &>/dev/null && echo "True" || echo "False"
[root] LOCAL STDOUT || True
[root] [ TiController Status ]
[root]  - Install Type: stand_alone
[root] -----------------------------------------------
[root]  - ticontroller (pid 105885) is running
[root]  - Web Node Info
[root]   + localhost.localdomain state: MASTER
[root]   + heap size: 1g
[root] -----------------------------------------------
[root]  - mysqld (pid 34320) is running
[root] -----------------------------------------------
[root]  - elasticsearch (pid 106105) is running
[root]  - Elasticsearch node status
[root]   + localhost.localdomain heap size: 1.0g <----해당 부분이 증설됨


/etc/sysconfig/elasticsearch
ES_HEAP_SIZE=1g 변경

변경 후 서비스 재시작 필요
systemctl restart elasticsearch.service
```
- 시험망 구성도 변경
- 스탠다드 교육
	- 교육 환경 구성 (usb 파일)
	- ppt 수정할 것 고치기
		- [x] 경보

26-03-19(목)
- [x] 교육 완료
	- 내일까지 채점 완료
- 주간 보고
	- kbs아트비전 장비 cli 멈춤 현상 추가 확인 필요 (하드웨어 관련 문제인지)
	- 전북대학교
		- 공유기 loop으로 인해 대량의 트래픽이 들어오게 되면 스위치 trusted port에서 처리 불가할 수 있음
		  (패킷 미러링해서 cpu에서 처리하는 방식인데 트래픽이 일정량을 넘어서 스누핑 처리 실패)
		- **다음주 화 OT 예정**
		- 경북대학교 - 기능 확인 필요
- 강원도립대학교 - 취득된 로그 연구소 공유 필요

26-03-20(금)
- [x] 강원도립대학교 연구소 로그 공유
- 경북대학교 기능 확인
	- [x] 답변 완료
```
DB에 더미 데이터 넣는 방법

mysql -u root -p cproject

CREATE TABLE dummy_table (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100),
    content TEXT,
    created_at DATETIME
);

DELIMITER $$
CREATE PROCEDURE insert_dummy(IN cnt INT)
BEGIN
    DECLARE i INT DEFAULT 0;
    WHILE i < cnt DO
        INSERT INTO dummy_table (name, email, content, created_at)
        VALUES (
            CONCAT('user_', i),
            CONCAT('user_', i, '@test.com'),
            REPEAT('dummy content data ', 500),
            NOW()
        );
        SET i = i + 1;
    END WHILE;
END$$
DELIMITER ;

CALL insert_dummy(100000);

------------
삭제 시
DROP PROCEDURE insert_dummy;
DROP TABLE dummy_table;
```
- A/S 출고 확인 (다음주 월 답변 남기기)