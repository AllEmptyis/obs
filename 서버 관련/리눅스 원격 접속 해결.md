# 용어 정리111
- X Window System
	- 리눅스/유닉스의 gui 시스템
- X11
	- X window system의 11번째 버전 (표준)
- Xorg
	- X11의 오픈소스 구현체 (x server)
- X Client
	- xserver를 통해 gui가 제공되어 지는 프로그램 (firefox 등)
- XRDP / VNC
	- X11 세션을 네트워크로 전달해주는 원격 GUI 도구
# xrdp
- 기존 설정 파일
```
[Unit]
Description=xrdp daemon
Documentation=man:xrdp(8) man:xrdp.ini(5)
Requires=xrdp-sesman.service
After=network-online.target xrdp-sesman.service

[Service]
Type=exec  ----> forking으로 변경 필요
EnvironmentFile=-/etc/sysconfig/xrdp
EnvironmentFile=-/etc/default/xrdp
ExecStart=/usr/sbin/xrdp $XRDP_OPTIONS
SystemCallArchitectures=native
SystemCallFilter=@system-service

[Install]
WantedBy=multi-user.target
```
- xrdp 구조
	- 유닉스 소켓: 유닉스 소켓은 OS 내부에서 파일 기반으로 통신한다
```
RDP Client (3389/TCP)
        ↓
   ┌────────────┐
   │  xrdp      │  ← TCP 소켓(3389) 수신
   └────────────┘
        ↓ (내부 연결)
   ┌────────────┐
   │ xrdp-sesman│  ← UNIX 소켓(/run/xrdp/sesman.socket)
   └────────────┘
        ↓
   ┌────────────┐
   │ Xorg/Xvnc  │
   └────────────┘
```
# 경로 정리
- 로그
	- /var/log/xrdp.log
- 설정 파일
	- /etc/xrdp/xrdp.ini
	- /etc/xrdp/sesman.ini
- 유닉스 도메인 소켓 파일 (휘발성)
	- /run/xrdp/sesman.socket
- xrdp 설정 파일
	- /usr/lib/tmpfiles.d/xrdp.conf
- sudo -u test bash -lc 'cd /etc/X11 && /usr/libexec/Xorg :12 -config xrdp/xorg.conf -noreset -nolisten tcp -verbose 3'
# xfce4 사용
- 설정 파일 변경 필요
```
/etc/xrdp/sesman.ini 

[Sessions]
UserWindowManager=/usr/bin/startxfce4
DefaultWindowManager=/usr/bin/startxfce4
```
- 패키지 다운로드
	- dnf -y groupinstall "Xfce"
- 시작 명령어
	- startxfce4
# vnc-any
- test / test1234
	- ! 사용 안 됨