```
3번 통신 안 되는 이유

라우터가 가지고 있는 네트워크 대역이 아니면 드롭시킴
->가능하게 하려면 L4등의 장비를 넣어서 slb시켜야 한다

ctl shift v


스토리지
- 기본 2벌 복제이기 때문에
전체 스토리지 용량의 반절 실제 사용 가능


물리 호스트 비밀번호는 반드시 hci 대시보드에서 변경
->커널단에서 바꾸게 되면 DB가 모를 수 있음


물리 네트워크 구성 ->8.0에서 바뀔 수 있음
멀티 프로바이더 구성
->멀티(여러개) 사이트의 구성망이 

vlan id = untag면 공란


디스크
- all flash (ssd만 사용) //권장
- ssd,hdd 둘 다 사용 -> 하이브리드

베이 갯수
ssd: 24베이 <-대부분 트레이에 더 많이 채울 수 있음
hdd: 8베이

디스크 추가
pvs는 분산 저장 -> 안에 있는 디스크 중 하나가 디스크가 작게 되어 있으면
실제 크기보다 작게 측정됨
그래서 최대로 설정 필요

마이그레이션
- 데이터는 어차피 분산저장되기 때문에 따로 복사 필요 없음
- 소요시간? 메모리 복사 시간에 따라 다름
메모리 128G 15분


라이브 마이그레션 -> 1개씩 (메모리 때문에)
보통 서버작업할 때 vm 다른 서버로 옮길 때 사용

플레이버
vm에 실제 할당할 수 있는 vcpu 갯수는 하이퍼
메모리는 초과해서 사용될 경우 oom 발생
->메모리 할당한 만큼 계속 사용

볼륨
->실제 데이터는 디스크에 저장되어 있음
그래서 볼륨을 바꾸면 다시 살아나기도..?


고가용성
메모리 70% 이상 사용 시 증설 권고

failback할 때는 유지보수를 꺼야 한다 ->안끄면 클러스터 생성 때문에 내부 시스템 문제 생길 수 있음


펌웨어 업데이트> 내부 컴포넌트
vm은 kvm에서 서비스되고 있기 때문에 서로 분리되어 있음

시스템의 각 항목은 컨테이너로 돌고 있음
```
